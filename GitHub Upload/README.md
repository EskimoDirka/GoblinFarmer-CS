# GitHub Upload

`GitHub Sync.bat` copies the latest published `GoblinFarmer.exe` into this folder before committing and pushing.

This is a convenience upload of the current executable only. Full installable builds still come from the publish folder, installer, portable zip, or an intentional GitHub Release.
