# GoblinFarmer Project Status

This file is the current source of truth for the active release, stable behavior, route/blocking rules, and next development plans. Historical notes and older package reviews live in `Docs/History.md`. Open work and validation items live in `Docs/TODO.md`.

## Current Release

- Version: `v1.4.0`
- Current focus: Goblin Tracker full automatic counting readiness in VS Debug.
- Automatic counting: implemented behind `GoblinTracker.EnableObservationMode=true` and `GoblinTracker.EnableAutomaticCounting=true`.
- Manual count hotkey: physical `X` has been retired. Goblin counts should come from automatic eligible evidence.
- VS Debug Goblin Tracker controls: `Observation Mode`, `Auto Goblin Count`, `Enable Goblin Decision Trace`, `Capture`, a debug-only simulation area selector, and `Sim Count`.
- VS Debug `Capture` button: manual image-recognition aid only. It writes fullscreen, minimap, journal, and metadata files under `Debug\GoblinEvidence\ManualCaptures` only when clicked.
- VS Debug `Sim Count` button: developer-only count-policy simulator. Its dropdown now uses a centralized route/policy area list built from `GoblinAreaResolver.KnownAreas`, with `Current Area` pinned first and the remaining route/count/block validation areas alphabetized. It uses existing area-key resolution, blocked-area checks, duplicate guard, and area-limit logic so accepted counts, duplicate suppression, and PF1/PF2/Stinging Winds `AreaLimitReached` behavior can be tested without waiting for rare live spawns.
- Automatic debug artifacts: accepted Goblin Tracker count workflows still automatically write decision bundles and encounter captures needed for debugging.
- `Next Tests` tab: removed. Current validation steps are tracked in `Docs/TODO.md`.
- Latest change: normal Goblin Evidence scanner events no longer write redundant root `GoblinEvidence_*` full/event images by default. Decision bundles and encounter captures now carry the replay-ready Journal/Minimap crops, metadata, JSONL, and trace data needed for debugging; the explicit VS Debug `Capture` button remains the only path that intentionally saves a fullscreen image for image-recognition troubleshooting.
- Latest detection policy change: automatic counting still prefers confirmed evidence. Minimap evidence remains eligible after the confidence gate, fresh Journal Killed evidence remains eligible, and first-seen Journal Engaged / combined Engaged-first evidence reports `JournalPendingKilledOrMinimapConfirmation`. If the same Engaged evidence remains in the same area during active combat for the short sustained-confirmation window, it may count as `JournalEngagedSustainedActiveCombat` so real goblins that never produce usable Killed/Minimap frames are not missed.
- Latest utility cleanup: normal tracked package launchers remain `Create Debug Package.bat` and `Create Project Brain.bat`. Required backing scripts are `create-debug-package.ps1` and `create-project-brain.ps1`; `debug-analysis-tools.ps1` remains because the debug package script directly loads it. `Cleanup Project.bat`, `Cleanup Project Delete.bat`, and `cleanup-project.ps1` are documented maintenance-only exceptions for generated artifact cleanup; the dry-run launcher and PowerShell script default to dry-run, and the delete launcher asks for `Y/N` before passing `-Delete`. Retired/manual helpers were moved to `Docs\ScriptArchive\2026-06-09` for reference. `create-project-brain.ps1 -StorageBreakdownOnly` writes `Reports\Storage_Breakdown.md` without deleting files.
- Latest agent-docs change: added lightweight DOX-style manuals under `Docs\Agent\Architecture.md`, `Workflows.md`, `Testing.md`, `Maintenance.md`, `Debugging.md`, and `Release.md`; shortened `AGENTS.md` into a directive index that points Codex to `Docs\Project_Status.md` as current source of truth and `Docs\Agent\*.md` as stable operating manuals; updated Project Brain generation to include `Docs\Agent\*.md`. No Agent Zero install, framework, or app logic change was introduced.

## Latest Review Finding

- Latest review source: user live-test notes plus `Blood Thief Pandemonium Fortress Level 2.mp4`.
- PF2 real multi-goblin bug found: the first `Blood Thief` in `Pandemonium Fortress Level 2` counted at about `06:00:42` with `GoblinAutoCountAccepted`, `areaCount=1`, `areaLimit=2`, and `total=6`. A second real `Blood Thief` was detected at about `06:00:52` with Minimap confidence around `0.865` plus Journal Engaged evidence, but it was suppressed as `Decision=Duplicate`, `Reason=EvidenceAlreadyAutoCounted`.
- Root cause: the PF2 area limit was correct, but exact evidence/encounter duplicate suppression ran before the remaining PF2 area slot could be used. Repeated same-type minimap templates can produce the same evidence signature/hash for separate real PF goblins.
- Fix implemented: PF1/PF2 now have a narrow multi-count duplicate bypass before exact evidence/encounter suppression. It applies only when the resolved area is `Pandemonium Fortress Level 1` or `Pandemonium Fortress Level 2`, the area limit is 2, the current area count is still below the limit, the previous accepted encounter was in the same PF level, at least 8 seconds elapsed since that accepted count, and the current scan has fresh supporting evidence from a strong Minimap candidate or sustained current-area Journal Engaged evidence. Normal one-count areas, previous-area stale Journal rows, immediate same-goblin duplicates, and third PF counts remain suppressed.
- Diagnostics added: accepted bypasses log `PfMultiCountDuplicateBypass` with area, area count/limit, elapsed time since last accepted count, source, goblin type, evidence hash, and short signature. Duplicate-shaped PF suppressions log `PfMultiCountDuplicateBypassSkipped` with the skip reason, including elapsed-too-short, area-limit reached, previous accepted area mismatch, or missing fresh supporting evidence.
- Tests added: explicit Goblin Replay template scenario covers PF2 first Blood Thief minimap count, immediate same-signature duplicate suppression, second same-signature Blood Thief count after the threshold, and third fresh PF2 goblin suppression as `AreaLimitReached`. Policy tests cover PF1 parity, PF2 second-count bypass, immediate duplicate suppression, third-count blocking, non-PF unchanged duplicate behavior, previous-area stale protection, sustained Journal Engaged support, and killed-Journal duplicate rejection.
- Video-derived replay validation passed against `Blood Thief Pandemonium Fortress Level 2.mp4`: extracted real Journal/Minimap crops from offsets `0s` and `8s` replayed as `Pandemonium Fortress Level 2`, both detected `Blood Thief` from `MinimapCandidate`, and both returned `decision=Count`, `reason=Eligible`, `counted=True`. Full per-second replay of the 15-second clip also produced two accepted PF2 Blood Thief counts and later duplicate Journal suppressions.
- Salvage cache review source: `Desktop 2026.06.09 - 16.31.24.07.DVR.mp4` plus `GoblinFarmer_Debug_20260609_163330.zip`.
- Salvage cache bug found: the single-scan cache kept the speed improvement, but the old whole-cell nonblank detector cached textured empty inventory cells and multi-slot item footprint cells as salvage targets. In the reviewed run, the log showed `cachedSlotCount=18`; the first 5 clicks found salvage confirmations, then slots 6-18 were clicked with `confirmationFound=False`. The matching DVR inventory crop visually showed 5 salvageable items.
- Fix implemented: salvage still captures the inventory grid once per open salvage UI cycle, but cache construction now uses item-anchor metrics instead of broad blank-tile rejection. Each grid cell logs a `Salvage cache candidate` line with row, column, screen point, accepted/rejected state, rejection reason, confidence, brightness/frame/content metrics, and `cacheMode=SingleInventoryScan`. Multi-slot vertical footprints collapse to one top-anchor click target, textured empty borders are rejected, and the existing per-slot confirmation checks remain unchanged.
- Tests added: synthetic salvage classifier coverage rejects textured empty cells, validates the DVR-style 5-item case caches 5 targets rather than 18-style false positives, deduplicates stacked multi-slot footprints, and preserves the one-scan salvage loop/timing diagnostics. Manual DVR-crop metric reproduction against the reviewed frame produced 5 accepted targets and rejected row-2 footprint cells.
- Follow-up salvage completion review source: `Desktop 2026.06.09 - 18.03.54.08.DVR.mp4` plus `GoblinFarmer_Debug_20260609_180426.zip`.
- Salvage completion bug found: the stricter cache was active and kept the single-scan path, but top cells for some two-slot items were rejected while lower footprint cells were accepted as anchors. The run logged `cachedSlotCount=9`, clicked all 9 cached targets, logged `confirmationMisses=9`, and still reported `Salvage completed`. Video review showed rare/yellow items salvaged instantly, while set/legendary-style items remained because expected confirmation prompts were not handled as required success conditions.
- Fix implemented: salvage cache construction now groups vertical item footprints by column and emits one top-cell target per footprint, including weak top cells attached to a strong lower footprint. Targets carry `footprintRows`, `quality`, and `confirmationExpected` metadata. Normal no-prompt items keep the fast confirmation probe, while set/legendary-style targets use a longer confirmation wait, retry once, press Enter when confirmation is found, and fail with a diagnostic screenshot instead of reporting success when an expected confirmation is missed.
- Tests added: weak-top two-slot items now target the top cell, textured empty cells still reject, DVR-style item anchors still cache without empty-cell expansion, stacked two-slot items stay separate, high-rarity synthetic items mark `confirmationExpected=true`, and salvage-loop source tests cover expected confirmation retry/failure, no-prompt continuation, one-scan cache behavior, and enhanced summary diagnostics.
- Latest reviewed package `GoblinFarmer_Debug_20260608_213351.zip` was a VS Debug run with Observation Mode, Auto Goblin Count, and Decision Trace enabled.
- Confirmed the prior latency fix in package evidence: Treasure Goblin in `Leoric's Hunting Grounds` counted from Minimap with fast timing. Latest live context reports detection-to-count about `13.3ms` and detection-to-notification display about `84.6ms`.
- Eastern Channel Level 2 miss review: the package does not show a fresh Eastern Channel Level 2 goblin candidate being rejected by the count policy. Minimap checks stayed below threshold, and Journal checks kept matching the old Leoric's Hunting Grounds Treasure Goblin row. When the Journal path ran candidate-specific area resolution, it detected `Eastern Channel Level 2` and correctly suppressed the old Leoric row with `JournalEngagedIgnoredAreaChanged`.
- Finding: some scan/freshness logs still showed `currentArea=Ancient Waterway` after the Eastern Channel detection because route/button context groups channel children under the Ancient Waterway route button. This was a diagnostics ambiguity in the reviewed package, not a proven stale-policy or latency regression.
- Diagnostic change implemented: Goblin Evidence scan, candidate-check, source-result, and Journal freshness logs now include route context fields: raw area, normalized area, display area, route group, button area, blocking area, current button, next button, and retry button. `JournalCandidateIgnoredStaleVisibleLine` also logs `areaResolutionSkippedReason=StaleVisibleLinePreAreaResolution` when it suppresses before candidate-specific area resolution.
- No counting behavior, stale suppression policy, notification latency path, combat, route, repair/salvage, Kadala, Battle.net, Start Game, replay, or debug package workflow changed in this diagnostic pass.
- Follow-up implementation: expanded VS Debug `Sim Count` area coverage, changed salvage to use one cached filled-slot inventory scan per open salvage UI cycle, and added count-based retention cleanup to `Scripts\create-debug-package.ps1` so batch-created debug packages retain 20 matching `GoblinFarmer_Debug_*.zip` files by default.
- Retention finding: the current local `DebugPackages` folder contained 21 matching packages before this change. Startup cleanup already used `DebugPackageRetentionCount=20`, but the batch export workflow did not immediately prune after creating a ZIP. The script now prunes the resolved export folder after package creation and logs deleted/skipped/kept counts.
- Post-validation live test result: PASS. Sim Count includes Eastern Channel Level 2, Eastern Channel Level 2 count simulation validated the count path, blocked-area suppression validated, cached salvage scan optimization validated, debug package retention validated, and no regressions were found.
- Post-validation cleanup: Sim Count dropdown now keeps `Current Area` first and alphabetizes all remaining centralized area keys. Tests now enforce the ordering and verify every `GoblinAreaResolver.KnownAreas` entry is present in Sim Count.
- No Goblin Tracker count, stale suppression, blocked-area, duplicate, area-limit, route, combat, repair/salvage, replay, or debug package workflow logic changed during the Sim Count ordering cleanup.
- Latest reviewed package `GoblinFarmer_Debug_20260608_204337.zip` was a VS Debug run with Observation Mode, Auto Goblin Count, and Decision Trace enabled.
- Counting validation passed in the package: the Northern Highlands Treasure Goblin counted correctly, old visible Journal rows did not recount, decision bundles and encounter captures were generated, and package size stayed healthy at about 12 MB.
- Root cause of the perceived notification latency: the splash notification was not the slow stage. The package shows strong Treasure Goblin Minimap evidence at `20:34:21.063`, then Treasure Goblin Journal Engaged evidence at `20:34:21.372`. Candidate selection preferred the pending Journal Engaged evidence, so automatic count waited until sustained active-combat confirmation around `20:34:23.978` before accepting the count and showing the notification at `20:34:24.051`. Count-to-notification display was only about 27 ms.
- Fix implemented: pending Journal Engaged evidence no longer masks a strong same-scan Minimap confirmation. When Journal Engaged and a strong Minimap candidate are both present, candidate selection chooses Minimap and logs `GoblinEvidenceCandidateSelection selected=Minimap reason=JournalPendingMinimapConfirmed`; the selected Minimap evidence still flows through the existing stale, block, duplicate, and area-limit guards.
- Added timestamped latency diagnostics: `GoblinLatencyTrace stage=EvidenceDetected`, `stage=CountAccepted`, `stage=NotificationQueued`, and `stage=NotificationDisplayed`. These report detection-to-count, count-to-notification, and detection-to-notification timings without changing count/suppression logic.
- Latest reviewed package `GoblinFarmer_Debug_20260608_193932.zip` plus the matching recording reproduced the Demon Hunter "stall" feeling in Northern Highlands around the 30-second mark.
- Package/video evidence showed combat remained active: right mouse stayed held, key spam continued, and the real combat stops in the package were explicit hotkey stops. The visible stall came from repeated left-click suppression while the physical cursor sat inside no-click UI regions such as `skill_bar`/HUD.
- Fix implemented: Demon Hunter now has a blocked-cursor fallback for the sustained right-hold path. When the cursor is in a no-click region but Demon Hunter right mouse was started from a safe region and is still held, the combat cursor loop can move to a verified safe playfield fallback point and send the left-click pulse there. This preserves the no-click blacklist while avoiding repeated HUD suppression stalls. Logs use `DemonHunterBlockedCursorFallbackLeftClickSent` or `DemonHunterBlockedCursorFallbackSkipped`.
- Goblin Tracker evidence in the same package found one useful validation: Eastern Channel Level 1 Blood Thief counted correctly from Journal Killed confirmation, reported `areaKey=Eastern Channel Level 1`, updated Last Observation, and suppressed follow-up same-evidence rows as already counted.
- Package size stayed healthy at about 11 MB.
- Latest reviewed package `GoblinFarmer_Debug_20260608_191706.zip` was a VS Debug run with Observation Mode, Auto Goblin Count, Decision Trace, and Demon Hunter combat enabled, plus a matching gameplay recording.
- Confirmed the package did not contain live goblins for the requested Leoric's Hunting Grounds or Cathedral Level 1 validation, but it did contain replay-ready Leoric decision bundles from high-confidence Treasure Goblin Engaged journal evidence.
- Found a wiring bug in the sustained-Engaged reliability path: Leoric's Passage Treasure Goblin Engaged evidence matured to `evidenceReliability=JournalEngagedSustainedActiveCombat` at about `2.4s`, but the final auto-count path still honored the earlier observation `wouldCount=False` / `JournalPendingKilledOrMinimapConfirmation` state and suppressed the count.
- Fix implemented: a pending Journal observation can now be promoted only when the reliability policy returns `JournalEngagedSustainedActiveCombat`. The promoted candidate still flows through the normal stale-evidence, blocked-area, duplicate, and area-limit guards before any count is accepted.
- Goblin Replay against decision bundle `gdt-20260609001602318-47c294a1` loads the replay-ready Leoric Journal/Minimap crops and correctly suppresses the first frame with `JournalPendingKilledOrMinimapConfirmation`; the multi-step harness scenario covers the later sustained-Engaged frame and now passes.
- Package/video Demon Hunter review: sampled recording frames and logs show Demon Hunter still attacking while "This skill is not ready" appears. User clarified that message is expected key-spam/cooldown feedback and should not be treated as a combat-stop cause. Click-safe regions suppressed only left clicks over HUD/buff regions while `combatRunning=True`, `combatActive=True`, and `rightMouseHeld=True`, then logged `DemonHunterClickSafeRecovery` when the cursor returned to a safe region. The only real combat stop in this package was an explicit combat-hotkey stop at `19:16:05`. Treat future Demon Hunter work as an unexpected-stop investigation only if a package shows combat stopping without hotkey/focus/teleport/app-close cause.
- Package size remained healthy at about 35 MB, and normal root full/event GoblinEvidence images remained absent from the package.
- Latest reviewed package `GoblinFarmer_Debug_20260608_185449.zip` was a VS Debug run with Observation Mode, Auto Goblin Count, and Decision Trace enabled.
- Confirmed Northern Highlands Blood Thief counted correctly from fresh Journal Killed evidence with `evidenceReliability=JournalKilledConfirmed`; count, notification, and Last Observation all updated.
- Confirmed Ruined Cistern Blood Thief eventually counted once fresh Killed journal evidence appeared. The initial delay was detection/policy latency rather than notification-window failure.
- Found two recall misses caused by the stricter Engaged-only reliability gate: Leoric's Hunting Grounds Treasure Goblin and Cathedral Level 1 Blood Thief were detected as high-confidence Journal Engaged evidence, but suppressed as `JournalPendingKilledOrMinimapConfirmation`. Leoric's Hunting Grounds showed the same Engaged line for several active-combat scans without a Killed/Minimap confirmation.
- Fix implemented: same-area Journal Engaged evidence can now auto-count only after it has stayed visible during active combat for the short sustained-confirmation window, logging `evidenceReliability=JournalEngagedSustainedActiveCombat`. The first Engaged sighting still stays pending, and old Engaged lines outside the sustained window remain non-counting.
- Fix implemented: a fresh same-area Journal Engaged line can re-anchor a later Journal Killed match for the current area, even if the same Killed template/row was previously seen stale in another area. This addresses the Cathedral pattern where fresh Blood Thief Engaged appeared, then Killed matched a stale Northern Highlands row and was ignored.
- Package evidence for stale transitions remained good: old Journal/Minimap variants in the reviewed run were suppressed as `EncounterAlreadyAutoCounted` or stale visible-line cases rather than recounting. Notifications no longer interfered with teleport clicks per live notes and can be treated as validated.
- Demon Hunter review: the latest package does not show click-safe regions directly stopping Demon Hunter combat. No-click regions suppressed only left clicks while `combatRunning=True`, kept right mouse held, and logged recovery. Observed combat stops were tied to explicit Escape/hotkey/teleport/focus/app-close paths. Next Demon Hunter work should add targeted unexpected-stop diagnostics before changing combat behavior.
- Latest reviewed package `GoblinFarmer_Debug_20260608_180056.zip` was a VS Debug run with Observation Mode, Auto Goblin Count, and Decision Trace enabled.
- Confirmed Leoric's Passage now counted correctly from fresh Minimap evidence: `Odious Collector` in `Leoric's Passage` accepted once, then Journal follow-up evidence suppressed as the same encounter/area.
- Confirmed Western Channel Level 1 now counted correctly: `Blood Thief` accepted with `areaKey=Western Channel Level 1`, notification shown, and Last Observation updated. The package also confirms the channel/Pandemonium ambiguity resolver selected Western Channel from route context even when PF1 was the best raw title match.
- Found a stale Journal row variant bug after Western Channel Level 1: the old Blood Thief killed row was correctly ignored as stale in Stinging Winds, but a nearby Blood Thief engaged-row template from the same visible journal history was later treated as fresh and consumed Stinging Winds count #1. A later killed-row variant consumed Stinging Winds count #2, and a third variant hit `AreaLimitReached`.
- Fix implemented: stale Journal suppression now records stale visible rows for both stale killed evidence and area-changed engaged evidence. New Journal candidates are checked against nearby same-goblin stale visible rows before they can become first-seen evidence. This suppresses old Engaged/Killed partner rows as `JournalCandidateIgnoredStaleVisibleLine` without weakening Minimap counting.
- Found a New Game carryover gap: Make New Game reset stats/state, but a previous-game Malevolent Tormentor killed Journal row was not visible/remembered at the exact reset moment and was accepted as a fresh Southern Highlands count about six seconds later. Fix implemented: `NewGameCreated` now arms a short Journal-only carryover suppression window so newly detected Journal rows immediately after New Game are logged/suppressed as `JournalCandidateIgnoredNewGameCarryoverWindow`; Minimap evidence remains eligible.
- Reliability refinement implemented: Journal Engaged-only candidates no longer increment automatic count. They remain visible diagnostic observations with reason `JournalPendingKilledOrMinimapConfirmation`, while fresh Journal Killed and Minimap evidence continue to count through the existing duplicate/stale/block/area-limit guards. Explicit Goblin Replay now shares this reliability policy and includes an Engaged-then-Killed confirmation scenario.
- The live notes mention a missed Malevolent Tormentor in Black Canyon Mines, but the current-session log/DecisionBundles in this package do not contain a Black Canyon Mines count decision for that encounter. The log does show Malevolent Tormentor checks around `17:55:26` below threshold (`0.793` vs `0.900` Journal and `0.574` vs `0.650` Minimap) while the current area was still in the Stinging Winds flow. Treat the Black Canyon miss as needing another package/capture if it repeats.
- Goblin Replay template scenarios now cover the two fixed policy shapes: Western Channel Blood Thief killed row -> Stinging Winds old killed row -> Stinging Winds old engaged partner suppresses, and late New Game previous-Journal carryover suppresses. Replay remains explicit/on-demand only.
- Latest reviewed package `GoblinFarmer_Debug_20260608_173101.zip` was a VS Debug run with Observation Mode, Auto Goblin Count, and Decision Trace enabled.
- Confirmed the package contained two accepted counts: Treasure Goblin in `The Weeping Hollow` and Gelatinous Sire in `Stinging Winds`. Last Observation persisted the accepted Gelatinous Sire count through later scans/teleports.
- Leoric's Passage Treasure Goblin did not count because the only detected Treasure Goblin Journal line was treated as old visible evidence first seen in `The Weeping Hollow` (`JournalEngagedIgnoredAreaChanged`). This still needs more evidence before loosening stale-Engaged handling, because the current protection is also what prevents old journal text from recounting after route transitions.
- The Stinging Winds -> Black Canyon Mines note showed up in package evidence as a later Gelatinous Sire Journal candidate resolved to `Battlefields`, with template `Gelatinous Spawn Killed Journal.png`, suppressed as `EncounterAlreadyAutoCounted` against the earlier `Gelatinous Sire Killed Journal.png` in Stinging Winds. Root cause: counted-encounter journal row matching used nearby row bucket plus normalized goblin type, but did not require the same journal line family/template. Fix implemented: `JournalLineBucket` encounter suppression now requires matching journal kind/template family as well as nearby row bucket. This preserves same-template stale-row protection while allowing fresh `Gelatinous Spawn`/`Gelatinous Sire` evidence in a new area to be evaluated separately.
- Goblin Replay against decision bundle `gdt-20260608222403763-776e9931` now loads the exact suppressed Gelatinous frame and returns `decision=Count reason=Eligible` under the updated policy, confirming the fix addresses that saved evidence path. The replay-resolved area remains `Battlefields` because that is what the bundle metadata/current live state recorded; a future package should confirm whether the user-visible area was Black Canyon Mines or whether route/current-area context advanced before the count decision.
- No Rainbow Goblin was counted in this package, so the special alert and sound still need live validation. Goblin Replay can exercise the count decision path, but it cannot validate actual Windows sound playback or click-through mouse behavior.
- Latest reviewed package `GoblinFarmer_Debug_20260608_164403.zip` was a VS Debug run with Observation Mode, Auto Goblin Count, and Decision Trace enabled.
- Confirmed package behavior matched live notes: Southern Highlands Gem Hoarder counted/notified correctly, Last Observation stayed persistent after accepted counts, Reset Stats cleared the displayed stats/state, and Pandemonium Fortress Level 2 Rainbow Goblin counted with the PF2 two-count limit still active.
- Found a Royal Crypts -> Sewers of Caldeum stale recount after a long pause: an Odious Collector counted in Royal Crypts, the visible killed Journal row stayed suppressed for several minutes, then the same row shifted from bucket 11 to bucket 8 and was accepted as a new Sewers count. Root cause was counted-encounter journal row matching using a stricter two-bucket tolerance than the reset-carryover journal policy. Fix implemented: counted-encounter Journal bucket matching now uses the shared four-bucket `GoblinJournalEvidencePolicy.NearbyLineBucketMaximumDelta`, with a focused test for the package-shaped `11 -> 8` pause/row-shift case.
- Western Channel Level 1 Treasure Goblin did not count in this package. Logs show high-confidence Treasure Goblin Journal text in Western Channel Level 1 was ignored as stale first-seen Northern Highlands evidence, then Western Channel Level 2 Treasure Goblin counted shortly after from fresh Minimap evidence. Treat the Western Channel Level 1 miss as needing one more live package/capture before changing freshness logic.
- Rainbow Goblin automatic counts now show a distinct `RAINBOW GOBLIN!` alert and play the local Windows exclamation sound. The notification splash window is now click-through at the Windows extended-style level so it should not block route/teleport mouse clicks.
- Latest reviewed package `GoblinFarmer_Debug_20260608_122007.zip` was a VS Debug run with Observation Mode, Auto Goblin Count, and Decision Trace enabled.
- Confirmed the Make New Game carryover fix in live package evidence: `Insufferable Miscreant` counted in `Southern Highlands` from fresh Minimap evidence, Make New Game reset stats/evidence state, then the old visible killed Journal row was suppressed in the new game with `JournalCandidateIgnoredResetCarryover` instead of recounting. The package shows the row moved from previous bucket 11 to current bucket 8 and stayed suppressed.
- No automatic count, notification, or Last Observation update occurred from the old previous-game Journal row after Make New Game, matching the live notes.
- Stinging Winds -> Black Canyon Mines and Black Canyon Mines -> Rakkis Crossing did not get live spawn coverage in this package. The explicit Goblin Replay template scenario from the prior validation covers those stale-Journal policy paths, so they are considered validated for now unless future live evidence contradicts them.
- Latest reviewed package `GoblinFarmer_Debug_20260608_120532.zip` was a VS Debug run with Observation Mode, Auto Goblin Count, and Decision Trace enabled.
- Stinging Winds and Rakkis Crossing did not spawn the needed live goblins, so those exact live validations remain open. Goblin Replay template scenarios were used instead to simulate the intended policy cases: old Eastern Channel Treasure Goblin Journal text suppresses in Stinging Winds, a fresh Stinging Winds Treasure Goblin Minimap count is still allowed, Black Canyon Mines Odious Collector Minimap counts, same-area duplicate Odious Journal evidence refreshes encounter state, and the old Odious Journal row suppresses after teleporting to Rakkis Crossing.
- Found a Make New Game carryover regression in the package: Blood Thief counted in `Northern Highlands`, Make New Game reset state, then the still-visible Blood Thief killed Journal row counted again after teleporting to `Southern Highlands`.
- Root cause: reset carryover suppression was armed and remembered two Journal rows, but the killed Journal line moved from bucket 11 to bucket 8 after the new game. The reset-carryover matcher allowed a two-bucket row movement, so the three-bucket shift was treated as a new first-seen row.
- Fix implemented: reset-carryover visible-line matching now allows a four-bucket movement while still requiring the same goblin/template line family. Goblin Replay template scenario validation now covers the exact `11 -> 8` Blood Thief New Game carryover shape and suppresses it as `JournalCandidateIgnoredResetCarryover`.
- Latest reviewed package `GoblinFarmer_Debug_20260608_114121.zip` was a VS Debug run with Observation Mode, Auto Goblin Count, and Decision Trace enabled.
- Confirmed Eastern Channel Level 1 is now corrected: a Treasure Goblin counted once and Last Observation/notification reported `Eastern Channel Level 1`, not Pandemonium Fortress Level 1.
- Found a Stinging Winds miss: the Stinging Winds Treasure Goblin did not count because stale Treasure Goblin Journal evidence from the earlier Eastern Channel Level 1 count refreshed the counted-encounter state, then a later fresh Stinging Winds Minimap hit was suppressed as `EncounterAlreadyAutoCounted`.
- Found a Rakkis Crossing stale recount: a Black Canyon Mines Odious Collector counted correctly from fresh Minimap evidence, but repeated same-area duplicate Journal evidence did not refresh the counted encounter's Journal row state; after teleporting to Rakkis Crossing, the same visible Odious Collector Journal row was accepted as a new Rakkis count.
- Fix implemented: cross-area stale Journal suppression no longer refreshes the counted encounter state, so an old Journal row cannot block a later fresh Minimap hit in the new area. Same-area duplicate Journal evidence now refreshes the counted encounter state, so a visible Journal row that was already tied to a counted area cannot become a fresh count after the next teleport. Goblin Replay template scenarios and direct policy tests cover the Stinging stale-Journal/fresh-Minimap case and the Rakkis same-row stale Journal guard.
- Confirmed Make New Game carryover behavior from the package: opening the old Journal text after Make New Game did not recount or update Last Observation.
- Latest reviewed package `GoblinFarmer_Debug_20260608_062030.zip` was a VS Debug run with Observation Mode, Auto Goblin Count, and Decision Trace enabled.
- Eastern Channel Level 1, Stinging Winds, and Rakkis Crossing did not spawn goblins in that run, so those route-specific live validations remain open.
- Confirmed the package matched the user's Make New Game note: Blood Thief counted in `The Weeping Hollow`, then Make New Game reset count/evidence state at `06:19:13`, then the still-visible Blood Thief killed Journal row was treated as fresh first-seen evidence in `Southern Highlands` and counted again at `06:19:19`.
- Root cause: `NewGameCreated` correctly cleared the duplicate guard and auto-count/evidence state, but clearing journal first-seen memory made already-visible previous-game Journal rows eligible again when they shifted upward in the feed under the new starting area.
- Fix implemented: Reset Stats/New Game now remembers visible Journal line families before clearing first-seen state and suppresses those reset-carryover rows while they remain visible, even if their row bucket moves slightly. This preserves the stat reset while preventing previous-game journal text from becoming a new-game count.
- Goblin Replay usability improvement implemented: the explicit test harness now supports `--goblin-replay-scenario` text files that synthesize temporary Journal/Minimap crop frames from `Images\Goblin Evidence` templates and can resolve area context from `Images\Current Location` templates through a shared current-location image resolver. The first targeted scenarios validate fresh Journal evidence before New Game, a New Game reset action, suppression of the same shifted visible Journal row as `JournalCandidateIgnoredResetCarryover`, Eastern Channel Level 1/2 independent area resolution, Stinging Winds -> Black Canyon stale-journal suppression, and Rakkis Crossing fresh minimap counting.
- Latest reviewed package `GoblinFarmer_Debug_20260608_055247.zip` was a VS Debug run with Observation Mode, Auto Goblin Count, and Decision Trace enabled.
- Confirmed Cave Of The Moon Clan Level 1 and Level 2 can count independently in the same game from fresh evidence: Level 1 Gem Hoarder counted from Journal, then Level 2 Gem Hoarder counted from Minimap and subsequent Level 2 Journal evidence suppressed as the same encounter.
- Confirmed Eastern Channel Level 2 can count independently after Level 1 stale evidence: Level 1 Blood Thief evidence was later suppressed on Level 2, then a fresh Eastern Channel Level 2 Treasure Goblin counted.
- Found an Eastern Channel Level 1/Pandemonium false-positive issue: a Blood Thief minimap candidate resolved to `Eastern Channel Level 1`, but the later Journal confirmation resolved/count accepted as `Pandemonium Fortress Level 1`, consuming a PF1 area slot and showing the wrong Last Observation area.
- Fix implemented: recent same-goblin Minimap observations in Eastern/Western Channel Level 1 or 2 now provide a short-lived area context for Journal follow-up evidence when the Journal current-location scan resolves to the matching Pandemonium Fortress level while route context is Ancient Waterway/channel. This preserves real PF1/PF2 behavior but prevents channel journal follow-ups from consuming PF slots.
- Goblin Replay against the Battlefields/Rakkis Gilded Baron decision bundles from the same package counted the fresh Battlefields evidence and suppressed the later Rakkis journal row as `EncounterAlreadyAutoCounted`, matching intended stale-transition behavior.
- Latest reviewed package `GoblinFarmer_Debug_20260607_212052.zip` was a VS Debug run with Observation Mode, Auto Goblin Count, and Decision Trace enabled.
- Confirmed package behavior matched live notes: Southern Highlands Treasure Goblin auto-counted correctly from fresh Minimap evidence, then Cave Of The Moon Clan Level 1 incorrectly auto-counted the stale Southern Highlands Treasure Goblin Journal row.
- Goblin Replay against the exact accepted Cave DecisionBundle `gdt-20260608022012960-08083e38` reproduced the bad decision as `Count/Eligible`, confirming this was a policy/state bug rather than only live timing.
- Root cause: after the fresh Southern Highlands Minimap count, the matching Journal row was suppressed as `RecentSourceVariant:Minimap->Journal`, but the remembered encounter state refreshed only source/last-seen data and kept the old Minimap evidence key. When the same Journal row was later detected after the area changed to Cave Of The Moon Clan Level 1, encounter suppression could not compare against a Journal evidence key and area duplicate logic accepted it as a new count. Suppressed source variants now refresh the encounter evidence key as well, so later visible Journal rows can be recognized as the same counted encounter.
- Goblin Replay scenario evaluation now mirrors the shared counted-encounter source-variant policy so an explicit multi-step replay can cover Minimap count -> suppressed Journal row -> later stale Journal row after an area transition.
- Latest reviewed package `GoblinFarmer_Debug_20260607_192644.zip` was a VS Debug form run with Observation Mode, Auto Goblin Count, and Decision Trace enabled. The package already contained the prior recent-last-seen suppression fix.
- Confirmed good behavior from the package: Highlands Cave, Weeping Hollow, Eastern Channel Level 1, and Eastern Channel Level 2 all produced accepted automatic counts; Eastern Channel Level 2 Gelatinous Sire counted quickly; accepted-count Last Observation persistence stayed active.
- Root cause of the Stinging Winds miss and Rakkis Crossing miss: fresh Minimap observations were detected, but `SameEvidenceKey` compared the area-independent Minimap icon signature against a much older same-type encounter in another area. Tight Minimap templates can naturally produce the same evidence signature across different maps, so Minimap-to-Minimap exact-key suppression is now same-area only.
- Root cause of the later Black Canyon Mines stale count: the fresh Stinging Winds Treasure Goblin Minimap hit was suppressed by that cross-area Minimap collision, then the delayed Treasure Goblin journal row appeared about 29 seconds later in Black Canyon Mines and was treated as eligible. Cross-source Minimap-to-Journal encounter suppression now uses a 45-second window to match the journal freshness window, so delayed journal rows after recent Minimap counts stay attached to the original encounter.
- The same package showed Rakkis Crossing Gem Hoarder Minimap evidence at high confidence (`0.972`) but suppressed as a stale Western Channel Level 1 Gem Hoarder by the same cross-area Minimap collision. The same policy fix should allow the fresh Rakkis count while still suppressing same-area duplicate Minimap scans.
- Notification latency remains mainly evidence-detection latency, not splash display latency. Timing summaries show a 500ms scanner interval with average total scan time around 460-470ms in this run. Continue live-validation after the stale/minimap collision fix before lowering thresholds or adding broader timing changes.
- VS Debug `Sim Count` controls remain useful for deterministic policy checks until full automatic counting has enough live confidence. Re-evaluate removal after remaining live auto-count validation is stable.
- Latest reviewed package `GoblinFarmer_Debug_20260607_183831.zip` showed Battlefields Treasure Goblin auto-counting correctly from fresh minimap evidence, then incorrectly counting stale Battlefields Treasure Goblin journal text as a new Fields of Slaughter count at `encounterAgeSeconds=20.8`.
- Root cause: same-encounter source-variant protection used only the original count timestamp for its 20-second recent-variant guard. The stale journal row was repeatedly suppressed until `19.6s`, refreshed encounter last-seen state, then slipped through just after the original count age crossed 20 seconds. The fix uses recent encounter `LastSeenUtc` continuity for source variants, while still keeping the suppression bounded so a later unrelated same-type goblin is not blocked forever.
- The same package showed Caverns of Frost Level 1 Gilded Baron auto-counting. User clarified Caverns of Frost Level 1 should allow one Goblin count even though it blocks Teleport Next route advancement; Caverns of Frost Level 2 should also allow one independent fresh count. Stale Level 1 evidence must still not become a Level 2 count.
- Last Observation was clearing repeatedly on no-candidate scans and area changes. Accepted manual, simulation, and automatic counts now publish a `Counted` Last Observation state that suppressed/stale/no-candidate scans and area changes preserve until Reset Stats, Make New Game, app restart, or a newer accepted count replaces it.

## Stable Systems

- Battle.net/Diablo launch, Start Game, Make New Game, route teleporting, interrupted teleport recovery, repair/salvage, Kadala timing, and Witch Doctor combat are stable enough for ongoing monitoring.
- Salvage uses a single inventory scan per open salvage UI cycle and caches actionable item anchors instead of every nonblank-looking grid cell. Multi-slot item footprints are collapsed to one top-cell click target, textured empty cells are rejected, and set/legendary-style targets are confirmation-aware so missed expected confirmations fail visibly instead of being reported as success.
- Release and VS Debug Goblin Tracker layouts keep evidence and Last Observation fields readable.
- Debug package workflow remains `Scripts\Create Debug Package.bat`, which creates a ZIP for VS Debug and Release review.
- Project Brain workflow is available through `Scripts\Create Project Brain.bat`, which delegates to `Scripts\create-project-brain.ps1` and writes a timestamped docs-only ZIP under `ProjectBrain` for quick AI-assisted development handoff context.
- Form close remains quiet and does not generate packages or loose review files.
- VS Debug and release/debug-mode artifact retention is 7 days; Goblin Evidence folders also use count/package limits.
- Debug package count retention is 20 matching `GoblinFarmer_Debug_*.zip` files by default. Startup cleanup and the batch debug-package export workflow both enforce the count against scoped `DebugPackages` folders.
- Debug ZIPs keep Goblin Tracker evidence replayable without carrying every full screenshot: Journal/Minimap crops, metadata, JSONL, logs, manifests, summaries, observation diagnostics, encounter captures, and decision traces are kept; normal root `GoblinEvidence_*` full/event images are skipped by default, and old full DecisionBundle evidence images plus capture fullscreen images are excluded from ZIPs by default and reported in the manifest.
- Tracked `Config\AppSettings.json` is sanitized for release/defaults. Private VS Debug paths and toggles should live in ignored `Config\AppSettings.local.json` when needed.
- Generated EXEs, installer output, portable ZIPs, debug packages, Project Brain ZIPs, logs, screenshots, source-upload output, and retired `GitHub Upload` copies are not source files.

## Goblin Tracker Current Behavior

- Observation scanner interval: `500ms`.
- Journal scan region: `64,736,645,417`.
- Minimap scan region: `2108,66,421,423`.
- Automatic counting requires Observation Mode and Auto Goblin Count to both be enabled.
- Automatic counting treats Minimap evidence and fresh Journal Killed evidence as count-confirming signals. Journal Engaged-only evidence is observation/diagnostic first, then may count only if the same Engaged evidence remains visible in the same area during active combat for the short sustained-confirmation window.
- Automatic counting uses existing area resolution, block list, duplicate guard, stale journal protection, and area-limit logic.
- Goblin Evidence scanning caches discovered template metadata and loaded OpenCV template mats, captures each source scan region once per pass, and scans Minimap before Journal. Journal remains primary when both sources are equally actionable, but a pending Journal Engaged match no longer masks a strong Minimap confirmation in the same scan.
- Goblin Replay Fixture Runner Phase 2E is implemented as explicit/on-demand harness support. Live scans still use `CopyFromScreen` by default, saved Journal/Minimap PNG fixtures can be fed through the shared frame/template matching path, and real saved encounter/manual capture folders can be loaded from the test harness command line for stale-location regression coverage without normal startup, VS Debug startup, scanner, route, combat, town, or debug package workflows invoking replay.
- Replay scenario evaluation uses the same `GoblinAutoCountEncounterSuppressionPolicy` as live automatic counting for counted-encounter source variants, while still remaining explicit/on-demand only.
- Goblin Replay supported inputs: shared capture folders through `--goblin-replay-captures`, specific `*_Metadata.txt` files through `--goblin-replay-metadata`, exact capture prefixes through `--goblin-replay-prefix`, DecisionBundle folders through `--goblin-replay-decision-bundle`, and synthetic template scenario files through `--goblin-replay-scenario`. DecisionBundle replay runs only when replay-ready Journal/Minimap capture frames can be resolved; otherwise it reports the trace/evidence files it found and explains the limitation. Template scenarios remain explicit/on-demand and create only temporary crop frames that are deleted after replay. Scenario `Location=...` steps can use current-location title PNGs to resolve the count area instead of manually specifying `Area=...`.
- New DecisionBundles are directly replay-ready through `--goblin-replay-decision-bundle` because each bundle carries its own metadata plus Journal/Minimap crops. Older bundles that contain only `decision-trace.txt` and fullscreen `evidence.png` remain manual-review evidence but are not crop-replayable unless matching capture frames exist elsewhere.
- Goblin Replay/policy coverage is strong enough to consider these validation paths covered unless a future live package contradicts them: Make New Game visible-row carryover suppression, Stinging Winds -> Black Canyon Mines stale-Journal suppression, Black Canyon Mines -> Rakkis Crossing stale-Journal suppression, Southern Highlands -> Cave Of The Moon Clan stale-Journal suppression, Cave/Caverns Level 1 -> Level 2 stale freshness protection, Battlefields journal-history row suppression, same-area duplicate suppression, and default/two-count area-limit policy.
- Goblin Replay does not validate live scanner latency, notification splash timing, Last Observation UI rendering/persistence, real Diablo focus/timing, route hotkey behavior, or package-size retention. Those still need normal VS Debug use/package review.
- Normal scanner evidence events record type/confidence/source and feed observation/auto-count, but skip the old root screenshot file. Replay/debug imagery should come from DecisionBundles, EncounterCaptures, ObservationDiagnostics, or the explicit manual `Capture` button.
- VS Debug `Sim Count` is not a replay feature and is not wired into live scanning. It is an explicit developer button that records through the existing session count path only when clicked.
- VS Debug `Sim Count` area options are centralized in `GoblinTrackerDebugSimulationAreas`. The list includes route count areas, special two-count areas, and blocked/no-count validation areas; the result still depends on existing block-list and duplicate-guard policy rather than a separate simulation mechanism.
- Goblin Evidence timing summaries log stage histograms through `GoblinEvidenceTimingSummary`.
- VS Debug/debug/decision-trace sessions write structured Goblin Tracker JSONL events to `Debug\GoblinEvidence\GoblinTrackerEvents.jsonl` alongside human-readable logs.
- Default area limit: 1 count per resolved area per game.
- Two-count exceptions: Pandemonium Fortress Level 1, Pandemonium Fortress Level 2, and Stinging Winds.
- PF1/PF2 same-signature duplicate handling has a scoped second-count bypass: a second real same-type goblin can count after the conservative 8-second threshold when a PF slot remains and current fresh Minimap or sustained Journal Engaged evidence supports it. Immediate duplicates and third PF counts still suppress.
- Blocked count areas include New Tristram, WhimsyDale, City of Caldeum, Gates of Caldeum, Caldeum Bazaar, Flooded Causeway, Ancient Waterway, and The Bridge Of Korsikk.
- Last Observation displays the most recent accepted goblin count after one exists. Suppressed, stale, duplicate, no-candidate, and area-change scans do not clear or replace that accepted-count display.
- Accepted-count notifications are no-activate and click-through. Rainbow Goblin automatic counts use a distinct alert message plus a local Windows exclamation sound.
- Treasure/Odious and Gilded/Malevolent minimap ambiguity pairs use color diagnostics/overrides.
- Reset Stats clears count, GPH/active time, duplicate guard, auto-count evidence state, observation state, and Last Observation.
- New Game clears count, GPH/active time, per-game duplicate/evidence state, observation state, and Last Observation so the next game starts clean.

## Sim Count Coverage Audit

Source checked: `GoblinAreaResolver.KnownAreas`, `GoblinManualCountBlockList`, and `GoblinTrackerDebugSimulationAreas.DropdownItems()`.

| Area key | Countable | Blocked | Present in Sim Count |
|---|---:|---:|---:|
| Ancient Waterway | No | Yes | Yes |
| Battlefields | Yes | No | Yes |
| Black Canyon Mines | Yes | No | Yes |
| Caldeum Bazaar | No | Yes | Yes |
| Cathedral | Yes | No | Yes |
| Cathedral Level 1 | Yes | No | Yes |
| Cathedral Level 2 | Yes | No | Yes |
| Cathedral Level 3 | Yes | No | Yes |
| Cave Of The Moon Clan Level 1 | Yes | No | Yes |
| Cave Of The Moon Clan Level 2 | Yes | No | Yes |
| Caverns of Frost Level 1 | Yes | No | Yes |
| Caverns of Frost Level 2 | Yes | No | Yes |
| City of Caldeum | No | Yes | Yes |
| Eastern Channel Level 1 | Yes | No | Yes |
| Eastern Channel Level 2 | Yes | No | Yes |
| Fields of Slaughter | Yes | No | Yes |
| Flooded Causeway | No | Yes | Yes |
| Gates of Caldeum | No | Yes | Yes |
| Hidden Camp | Yes | No | Yes |
| Highlands Cave | Yes | No | Yes |
| Leoric's Hunting Grounds | Yes | No | Yes |
| Leoric's Passage | Yes | No | Yes |
| New Tristram | No | Yes | Yes |
| Northern Highlands | Yes | No | Yes |
| Pandemonium Fortress Level 1 | Yes | No | Yes |
| Pandemonium Fortress Level 2 | Yes | No | Yes |
| Rakkis Crossing | Yes | No | Yes |
| Royal Crypts | Yes | No | Yes |
| Ruined Cistern | Yes | No | Yes |
| Sewers of Caldeum | Yes | No | Yes |
| Southern Highlands | Yes | No | Yes |
| Stinging Winds | Yes | No | Yes |
| The Bridge Of Korsikk | No | Yes | Yes |
| The Festering Woods | Yes | No | Yes |
| The Weeping Hollow | Yes | No | Yes |
| Western Channel Level 1 | Yes | No | Yes |
| Western Channel Level 2 | Yes | No | Yes |
| WhimsyDale | No | Yes | Yes |

Result: no missing countable areas. Blocked areas are intentionally present so `BlockedArea` suppression can be validated from the same dropdown. `Current Area` remains a pinned synthetic option and is not an area key.

## Retention Policy Audit

Configured values:

- General debug artifact age retention: `AppSettings.RetentionDays=7`, exposed as `AppSettings.DebugArtifactRetentionAge`.
- Session summaries: `Debug.SessionSummaryRetentionCount=50`.
- Debug packages: `Debug.DebugPackageRetentionCount=20`.
- Goblin Evidence loose files: `Debug.GoblinEvidenceRetentionCount=250`.
- ObservationDiagnostics crop samples: `GoblinEvidenceObservationDiagnosticRetentionCount=24`.
- Debug package export defaults: `DebugPackageRetentionCount=20`, `MaxGoblinObservationDiagnosticCrops=6`, `MaxGoblinEvidenceFullImages=0`, `MaxGoblinEvidenceEventScreenshots=3`, `MaxGoblinEvidenceEventScreenshotBytes=1048576`.

Cleanup triggers and behavior:

- Startup runs `DebugManager.CleanupDebugArtifactsByAge`, `CleanupOldSessionSummaries`, `CleanupOldDebugPackages`, and `CleanupOldGoblinEvidence`.
- Age cleanup targets `Logs`, `Screenshots`, `debug-screenshots`, `Sessions`, `DebugPackages`, and `Debug\GoblinEvidence` under both the runtime base directory and resolved project/config root when available.
- Session summary count cleanup keeps newest matching `Sessions\Session_*.md` files.
- Debug package count cleanup keeps newest matching `DebugPackages\GoblinFarmer_Debug_*.zip` files; startup scans discovered `DebugPackages` folders and `Scripts\create-debug-package.ps1` also prunes the resolved export folder immediately after a batch-created ZIP is written.
- GoblinEvidence count cleanup keeps newest loose files recursively under `Debug\GoblinEvidence`; it is called at startup and after calibration snapshot completion. ObservationDiagnostics also runs its own 24-file cleanup during scanner passes.
- DecisionBundles, EncounterCaptures, ManualCaptures, and replay-ready crops are included in the GoblinEvidence tree and are therefore covered by the startup age/count cleanup. Debug package ZIP contents are additionally bounded by package-script include/exclude limits.
- Goblin Replay template scenarios create only temporary crop frames and delete their temp root at the end of explicit harness execution; Replay is not a normal startup, scanner, package, or form-close cleanup trigger.

Result: actual cleanup trigger locations match the configured policy. No code mismatch found.

## Route Logic

- Southern Highlands -> Northern Highlands.
- Cave Of The Moon Clan Level 1 blocks Teleport Next; Cave Of The Moon Clan Level 2 is allowed as a Southern Highlands sublocation.
- Northern Highlands -> The Weeping Hollow -> The Festering Woods -> Cathedral.
- Cathedral blocks Royal Crypts except Cathedral Level 3.
- Royal Crypts -> City Of Caldeum.
- City Of Caldeum blocks Ancient Waterway except Ruined Cistern.
- Ancient Waterway -> Stinging Winds. Plain Ancient Waterway blocks Teleport Next to Stinging Winds.
- Western Channel Level 1 blocks; Western Channel Level 2 returns to Ancient Waterway.
- Eastern Channel Level 1 blocks; Eastern Channel Level 2 allows Stinging Winds.
- Stinging Winds blocks Battlefields except Black Canyon Mines.
- Battlefields -> Rakkis Crossing. Caverns of Frost Level 1 blocks; Caverns of Frost Level 2 allows Rakkis Crossing.
- Rakkis Crossing -> Pandemonium Fortress Level 1 -> Pandemonium Fortress Level 2 -> Make New Game.

## Next Development Plans

- Continue validating real auto-count during normal VS Debug use, with special attention to Level 2 area independence, stale journal/location transitions, and notification latency. For latency, check whether strong Minimap plus Journal Engaged scans log `GoblinEvidenceCandidateSelection reason=JournalPendingMinimapConfirmed` and whether `GoblinLatencyTrace` shows low count-to-display time.
- Next Eastern Channel Level 2 validation should focus on whether a fresh goblin produces any Minimap hit above threshold or fresh Journal Killed/sustained Engaged evidence while route logs show `routeRawArea=Eastern Channel Level 2`. If it misses again, compare the new route context fields against `GoblinTracker: Area resolved` and `GoblinEvidenceScanResult` before changing stale suppression.
- The Make New Game previous-Journal-row recount fix is validated by both Goblin Replay and `GoblinFarmer_Debug_20260608_122007.zip`; reopen only if a future package shows a new reset carryover shape.
- Stinging Winds -> Black Canyon Mines and Black Canyon Mines -> Rakkis Crossing stale-Journal transitions are validated for now by explicit Goblin Replay. Continue normal play/use monitoring instead of forcing those niche spawns.
- Treat route-specific stale-Journal policy scenarios as replay-validated going forward when they can be modeled from saved Journal/Minimap/current-location frames. Use live testing primarily for real detection misses, type misclassification, notification latency, Last Observation display, and cases where replay output disagrees with the actual package story.
- Use VS Debug `Sim Count` when a rare count-policy edge case needs deterministic verification, especially PF1/PF2/Stinging Winds third-count suppression.
- Next Goblin Replay work should use the explicit harness command against real capture folders, specific metadata files/prefixes, DecisionBundle folders, or small `--goblin-replay-scenario` files from suspicious live sessions and only promote narrowly proven stale-location fixes into production code. Replay can now cover image-template area resolution plus count policy, but live runs are still needed for scanner timing, notification latency, and whether real in-game frames arrive before/after transitions.
- Latest replay validation used `--goblin-replay-metadata` against the `GoblinFarmer_Debug_20260607_175901.zip` Battlefields manual Capture and accepted EncounterCapture. Latest Sim Count validation used loose VS Debug logs from `GoblinFarmer_20260607_181728.log`; no new debug package was created for that run.
- Continue using automatic counting in real runs instead of focused specific-goblin hunts.
- Use the `Capture` button only when an image-recognition issue is visible and extra minimap/journal/fullscreen evidence would help.
- Keep `Docs/TODO.md` synchronized with remaining work and next test steps.
- Next validation should confirm both loose runtime storage and the next package are smaller while still containing replay-ready DecisionBundles, EncounterCapture Journal/Minimap crops, manifests, JSONL, and analysis files.
- Next town-workflow validation should confirm salvage scans the inventory once, logs `cacheMode=SingleInventoryScan`, logs per-cell `Salvage cache candidate` decisions with `quality`, `footprintRows`, and `confirmationExpected`, caches only real top-cell item anchors, presses Enter for set/legendary confirmations, and completes without bad empty-cell clicks or missed expected confirmation prompts.
- Next debug-package validation should confirm the package script logs `Debug package retention cleanup complete` and retains only the newest 20 matching `GoblinFarmer_Debug_*.zip` files in the resolved `DebugPackages` folder.
- Next PF1/PF2 live validation should target a real two-goblin encounter in each level: verify the first and second real goblin both count, look for `PfMultiCountDuplicateBypass` only on the delayed second same-signature case, and verify a third PF count still suppresses as `AreaLimitReached`.

## Potential Next Major Features

- Automatic gem stashing workflow after repair/salvage/town automation remains stable.
- Additional Goblin Tracker detection hardening for low-confidence Minimap frames, route-title ambiguity, and rare Journal line variants.
- Replay-assisted evidence diagnostics that summarize suspicious saved evidence while keeping Goblin Replay explicit/on-demand.
- Release readiness items: final VS Debug confidence pass, package-size check, installer/publish verification, release notes, and GitHub release artifact validation.

## Developer Utilities

- Current tracked `Scripts` policy: the normal user-facing package batch files are `Scripts\Create Debug Package.bat` and `Scripts\Create Project Brain.bat`.
- Maintenance exception: `Scripts\Cleanup Project.bat`, `Scripts\Cleanup Project Delete.bat`, and `Scripts\cleanup-project.ps1` are tracked temporarily for generated artifact cleanup. The dry-run launcher and cleanup script default to dry-run. The delete launcher requires a `Y/N` confirmation before calling the cleanup script with `-Delete`.
- Required backing scripts: `Scripts\create-debug-package.ps1` and `Scripts\create-project-brain.ps1`.
- Required dependency/helper: `Scripts\debug-analysis-tools.ps1`, because `create-debug-package.ps1` dot-sources it to write `debug-package-analysis.txt`, `goblin-tracker-timeline.md`, and `goblin-evidence-health.txt` into debug ZIPs.
- Archived scripts: `Git Commit Push.bat`, `publish-release.ps1`, `analyze-latest-debug-package.ps1`, `build-goblin-tracker-timeline.ps1`, `check-goblin-evidence-health.ps1`, and `dev-verify.ps1` were moved to `Docs\ScriptArchive\2026-06-09` for reference. They are candidates for later deletion after manual review.
- Scripts not found in the active tree: `Git Commit Push.ps1`, `GitHub Sync.bat`, and `GitHub Sync.ps1`.
- Local-only ignored area: `Scripts\Local Tools\*` is not tracked and remains manual-review/private workspace content outside the tracked script policy.
- Project Brain purpose: create a lightweight docs-only `GoblinFarmer_ProjectBrain_YYYYMMDD_HHMMSS.zip` package under `ProjectBrain` for AI-assisted development context without runtime/debug artifacts, while keeping this file as the source of truth.
- Project Brain how to run: from the repository root, launch `Scripts\Create Project Brain.bat` or run `powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\Scripts\create-project-brain.ps1`.
- Project Brain included files are allowlisted only: `AGENTS.md`, `README.md`, `Docs\Project_Status.md`, `Docs\ProjectBrain\*.md`, `Docs\Agent\*.md`, selected small stable docs such as `Docs\TODO.md`, `Docs\History.md`, `Docs\CombatProfiles.md`, `Docs\TeleportLogic.md`, `Docs\ScanRegions.md`, `Docs\Release_Checklist.md`, `Docs\Release_v1.4.md`, and optional `Docs\Test_Results.md`, `Docs\Known_Issues.md`, `Docs\Next_Tasks.md`, and `Docs\Release_Notes.md` when they exist.
- Agent docs: `Docs\Agent\*.md` contains stable agent operating manuals for architecture, workflows, testing, maintenance, debugging, and release. Codex should read the relevant file before touching related areas. These docs summarize stable rules only; `Docs\Project_Status.md` remains the current source of truth and wins on conflict.
- Storage report workflow: run `powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\Scripts\create-project-brain.ps1 -StorageBreakdownOnly`. It writes `Reports\Storage_Breakdown.md` and does not delete files.
- Storage report contents: total folder size, top-level folder sizes, largest subfolders, largest file extensions, top largest files, source/docs vs generated/debug/build split, and likely cleanup targets including `bin`, `obj`, `DebugPackages`, `Debug`, `GoblinEvidence`, `DecisionBundles`, logs, screenshots, installers/packages, and replay captures.
- Cleanup script usage: dry-run with `Scripts\Cleanup Project.bat` or `powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\Scripts\cleanup-project.ps1`; delete default generated targets with confirmation through `Scripts\Cleanup Project Delete.bat`, or directly with `powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\Scripts\cleanup-project.ps1 -Delete`; optional runtime cleanup requires direct PowerShell with `-RuntimeArtifacts`; debug package pruning uses `-DebugPackageRetention 10` by default; old installer pruning uses `-PruneOldInstallers`, which keeps the newest installer.
- Cleanup protected paths: `Images`, `Docs`, `Scripts`, `Config`, `Installer`, `.git`, source files, project/solution files, `README.md`, `AGENTS.md`, and Project Brain docs.
- Cleanup report workflow: `Scripts\cleanup-project.ps1` writes `Reports\Cleanup_Report.md`, logs every path considered, estimates reclaimable bytes, skips missing paths, refuses targets outside the project root, and verifies the expected project root markers before considering deletion.
- Recommended cleanup order: run dry-run first; clean `bin`, `obj`, and `Tests` build outputs; reduce debug package retention if needed; only then decide separately whether `-RuntimeArtifacts` or `-PruneOldInstallers` should be used.
- What changed: active script surface was simplified, obsolete/manual scripts were archived, `.gitignore`, README, Project Brain docs, release checklist, installer messages, project publish validation, and script-policy tests were updated, the Project Brain PowerShell script gained storage-report mode and now includes `Docs\Agent\*.md`, the cleanup maintenance script was added with dry-run/delete guardrails, `Docs\Agent` manuals were added, and `AGENTS.md` was shortened into a directive index.
- Tested: `Scripts\Create Debug Package.bat` created `DebugPackages\GoblinFarmer_Debug_20260608_231533.zip` and performed expected retention by deleting the prior oldest matching ZIP to keep 20 packages; `Scripts\Create Project Brain.bat` created `ProjectBrain\GoblinFarmer_ProjectBrain_20260608_231551.zip`; after adding `Docs\Agent`, `Scripts\create-project-brain.ps1` created `ProjectBrain\GoblinFarmer_ProjectBrain_20260609_053924.zip` and ZIP contents were verified to include all six `Docs\Agent\*.md` files; `create-project-brain.ps1 -StorageBreakdownOnly` created `Reports\Storage_Breakdown.md`; `Scripts\cleanup-project.ps1` dry-run completed against the real repo, wrote `Reports\Cleanup_Report.md`, estimated about 1.73 GB reclaimable, and deleted 0 bytes; cleanup delete mode was validated only against a temp fixture with `-Delete -RuntimeArtifacts -DebugPackageRetention 1 -PruneOldInstallers`, confirming generated paths were deleted, newest debug package/latest installer were kept, protected paths remained, and the fixture report was created; `Scripts\Cleanup Project Delete.bat` was validated with an `N` response and cancelled before invoking `cleanup-project.ps1 -Delete`; `dotnet build GoblinFarmer.csproj` passed; `dotnet build .\GoblinFarmer.csproj -p:UseSharedCompilation=false` passed; `dotnet run --project .\Tests\GoblinFarmer.Tests\GoblinFarmer.Tests.csproj -p:UseSharedCompilation=false` passed; `git diff --check` passed with line-ending normalization warnings only.
- Still needs testing: clean-checkout Project Brain package generation and any future release publish/installer pass should be validated after the active release workflow is exercised.
- Next recommended task: use the new `Docs\Agent` manuals as the first stable operating reference for future Codex tasks, then continue with the cleanup-report review when storage cleanup is desired.
